<%= form_for(@dog) do |f| %>
	<%= render 'shared/error_messages', object: f.object %>

	<%= f.label :name %>
	<%= f.text_field :name %>

		
	<%= f.submit "Create dog", class: "btn btn-large btn-primary" %>
<% end %>